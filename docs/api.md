# API reference

Document the package's stable public API here. Include parameter types, return
types, thrown exceptions, and a focused example for each public entry point.

## `Package::name()`

Returns the package name.

```php
$name = YourVendor\YourPackage\Package::name();
```
